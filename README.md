# Modhus - AI for All

A beautiful landing page for Modhus, the YouTube for AI models. Built with Next.js, TypeScript, and Tailwind CSS.

## Features

- 🎨 Modern, responsive design with animated gradient backgrounds
- 🎵 Interactive audio player with waveform visualization
- 🎭 3D model viewer using Google's model-viewer
- 📧 Waitlist signup with Supabase backend integration
- ⚡ Fast and optimized with Next.js 14
- 🎯 Type-safe with TypeScript

## Getting Started

### Prerequisites

- Node.js 18+ installed
- A Supabase account (for waitlist functionality)

### Installation

1. Clone the repository:
```bash
git clone <your-repo-url>
cd modhusweb
```

2. Install dependencies:
```bash
npm install
```

3. Set up Supabase (see [SUPABASE_SETUP.md](./SUPABASE_SETUP.md) for detailed instructions):
   - Create a Supabase project
   - Run the SQL setup script
   - Copy your credentials to `.env.local`

4. Create `.env.local` and add your Supabase credentials:
```env
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

5. Run the development server:
```bash
npm run dev
```

6. Open [http://localhost:3000](http://localhost:3000) in your browser

## Project Structure

```
modhusweb/
├── app/
│   ├── api/
│   │   └── waitlist/
│   │       └── route.ts          # Waitlist API endpoint
│   ├── components/
│   │   └── logo.jsx              # Modhus logo component
│   ├── globals.css               # Global styles & animations
│   ├── layout.tsx                # Root layout
│   └── page.tsx                  # Main landing page
├── lib/
│   └── supabase.ts               # Supabase client configuration
├── public/
│   └── assets/
│       └── demo/                 # Demo files (3D models, audio)
├── supabase-setup.sql            # Database setup script
├── SUPABASE_SETUP.md             # Detailed setup guide
└── package.json
```

## Tech Stack

- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **Icons**: Lucide React
- **Database**: Supabase (PostgreSQL)
- **3D Viewer**: Google Model Viewer
- **Font**: Roboto (Google Fonts)

## Demo Assets

The demo section showcases:
- **3D Model**: A retro car generated by `tencent/Hunyuan3D-2.1`
- **Music**: Techno beat generated by `facebook/musicgen-small`

## Deployment

### Deploy to Vercel

1. Push your code to GitHub
2. Go to [vercel.com](https://vercel.com)
3. Import your repository
4. Add environment variables in Vercel dashboard
5. Deploy!

### Environment Variables for Production

Make sure to add these in your hosting platform:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

## Scripts

- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm run start` - Start production server
- `npm run lint` - Run ESLint

## License

Private and proprietary.